{
  "name": "root",
  "private": true,
  "workspaces": [
    "./packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "demo-site": "lerna run dev --stream --scope demo-site",
    "format": "lerna run format --stream",
    "lint": "lerna run lint --stream",
    "test": "lerna run test --stream",
    "type-check": "lerna run type-check --stream"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
