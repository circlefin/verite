import { randomDidKey } from "../../lib/utils"

describe("client e2e", () => {
  it("handles manfiest and requests for credentials", async () => {
    // Client: The subject gets a DID
    const didKey = randomDidKey()
  })
})
